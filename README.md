# Experimental Daily Country GPR

This folder is a separate prototype page for the future TDM Studio dataset.

Expected `data.csv` columns:

```csv
date,country,iso3,total_articles,gpr_articles,country_gpr_articles,raw_share,index
```

Do not put newspaper article text here. Only publish derived aggregate counts and index values.
